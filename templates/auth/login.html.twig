{% extends 'base.html.twig' %}

{% block title %}Inicio de Sesión{% endblock %}
{% block stylesheets %}
 <link rel="stylesheet" href="{{ asset('styles/login.css') }}">
 {% endblock %}
{% block body %}
<section class="container-form">
    <div class="btn-back-content" style="margin: 5px 2px; text-align:start;">
        <a class="back_btn" href="javascript:history.go(-1);" type="button">
            <i class="fa-solid fa-chevron-left"></i> Regresar
        </a>

    </div>
    <div class="titulo-con-lineas">
        <span class="titulo">Inicio de sesión</span>
    </div>
    <form method="post" class="form_container">
        <div class="form-login">

            <div class="login_form">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-3">
                        You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                {% endif %}
                <label for="username">Correo</label>
                <input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control input_login" autocomplete="email" required autofocus>
                <label for="password">Contraseña</label>
                <input type="password" name="_password" id="password" class="form-control input_login" autocomplete="current-password" required>

                <input type="hidden" name="_csrf_token"
                    value="{{ csrf_token('authenticate') }}">
                <div class="content-btn-login">
                    <button class="btn btn-lg btn-primary btn-login" type="submit">
                        Acceder
                    </button>
                </div>
            </div>
            <div style="text-align: end;">
                <img class="img_login" src="https://wallpapers.com/images/hd/food-4k-3gsi5u6kjma5zkj0.jpg" >
            </div>
        </div>
    </form>
    </section>
{% endblock %}
